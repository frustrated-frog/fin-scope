spring:
  datasource:
    url: jdbc:sqlite:/data/finance.db?foreign_keys=on
  redis:
    host: redis
    port: 6379
    timeout: 1000ms
  kafka:
    bootstrap-servers: kafka:19092

finscope:
  data-root: /data
  cors-origin: http://localhost:5173
  python-market-data:
    base-url: http://market-data:8000
