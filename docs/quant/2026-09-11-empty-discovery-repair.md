# 盘前空报告遮挡候选修复

2026-09-11 07:58 触发的 SCHEDULED 批次 4006 请求 9 月 11 日收盘数据，实际行情截至 9 月 10 日。296 只候选均未通过准入，深度预测为零，报告仍被持久化为 SUCCEEDED。

这份业务日期较新的空报告遮挡了批次 3396 的完整报告，并使当日收盘调度因成功状态而直接返回。源码默认定时为 15:30、启动恢复也会回退日期；本次没有足够证据确定早间 SCHEDULED 触发的具体来源，因此不将其归因于默认恢复逻辑。

修复在业务服务完成报告前核对 asOfDate 与请求业务日期；不一致进入现有失败重试流程，不能作为成功报告替换页面。日期正确、真正没有合格候选的报告仍可正常完成，不放宽股票准入或模型门槛。

现存批次 4006 原始记录已在本机备份，并以限定 ID、原状态和日期差异的条件更新为 FAILED；原始报告、候选和时间戳保留。页面随即恢复批次 3396：9 月 10 日收盘、15 只深度研究、5 只观察候选、严格入选 0。其下一交易日预测目标为 9 月 11 日，不是下周一，也不代表盘中买入至下周一的可成交收益。

验证：新增回归测试先复现旧代码不拒绝错误日期；修复后 Service 7 项、Controller 3 项、Listener 1 项全部通过。后端重启后 API 与浏览器验证候选恢复，当前失败批次等待收盘调度重试。未变更用户已有配置与依赖锁文件。
