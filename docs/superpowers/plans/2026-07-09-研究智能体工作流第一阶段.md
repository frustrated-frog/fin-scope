# Research Agent 工作流第一阶段实现计划

> **给智能体执行者：** 必须使用子技能：使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`，按任务逐项执行本计划。步骤使用复选框（`- [ ]`）语法跟踪。

**目标：** 新增持久化的研究运行计划状态，并在 Research 标签页展示。

**架构：** 本阶段新增 `research_run_plan` 表、轻量领域对象、repository 和用于步骤状态的 service。`ResearchService` 在保留当前确定性 source-fetch 与简报生成流程的同时初始化并更新计划步骤。Web 响应和 React 类型/视图新增 `planSteps`，并在运行详情面板中展示更丰富的追踪元数据。

**技术栈：** Java 8、Spring Boot 2.7、SQLite/JdbcTemplate、JUnit 5、Mockito、React、TypeScript、Vitest、Testing Library。

---

## 文件结构

- 新增 `backend/finscope-domain/src/main/java/com/finscope/domain/research/ResearchRunPlanStep.java`
  - 用于持久化计划步骤状态的 domain DTO。
- 新增 `backend/finscope-dao/src/main/java/com/finscope/dao/research/ResearchRunPlanRepository.java`
  - `research_run_plan` 的 SQL 持久化和 row mapping。
- 新增 `backend/finscope-service/src/main/java/com/finscope/service/research/ResearchRunPlanService.java`
  - 计划步骤的业务级状态流转。
- 新增 `backend/finscope-service/src/test/java/com/finscope/service/research/ResearchRunPlanServiceTest.java`
  - 默认计划和状态流转的单元测试。
- 修改 `backend/finscope-dao/src/main/java/com/finscope/dao/config/DatabaseInitializer.java`
  - 创建 `research_run_plan` 和索引。
- 修改 `backend/finscope-domain/src/main/java/com/finscope/domain/research/ResearchRunPlan.java`
  - 新增 `planSteps`。
- 修改 `backend/finscope-service/src/main/java/com/finscope/service/research/ResearchService.java`
  - 在运行执行期间初始化并更新计划步骤。
- 修改 `backend/finscope-service/src/test/java/com/finscope/service/research/ResearchServiceHarnessTest.java`
  - 验证创建运行时会初始化并返回计划步骤。
- 修改 `backend/finscope-web/src/main/java/com/finscope/web/response/ResearchRunDetailResponse.java`
  - 包含 `planSteps`。
- 修改 `backend/finscope-web/src/main/java/com/finscope/web/controller/ResearchController.java`
  - 将计划步骤传入详情响应。
- 修改 `backend/finscope-web/src/test/java/com/finscope/web/FinScopeApiIntegrationTest.java`
  - 验证详情 API 返回 `planSteps`。
- 修改 `frontend/src/shared/types/index.ts`
  - 新增 `ResearchRunPlanStep` 和增强后的 `AgentRun` 字段。
- 修改 `frontend/src/features/research/ResearchView.tsx`
  - 渲染计划步骤和 trace metadata。
- 修改 `frontend/src/App.test.tsx`
  - mock `planSteps` 并断言 UI 会渲染它们。

## 任务 1：计划步骤服务

**文件：**
- 新增： `backend/finscope-domain/src/main/java/com/finscope/domain/research/ResearchRunPlanStep.java`
- 新增： `backend/finscope-dao/src/main/java/com/finscope/dao/research/ResearchRunPlanRepository.java`
- 新增： `backend/finscope-service/src/main/java/com/finscope/service/research/ResearchRunPlanService.java`
- 新增： `backend/finscope-service/src/test/java/com/finscope/service/research/ResearchRunPlanServiceTest.java`
- 修改： `backend/finscope-dao/src/main/java/com/finscope/dao/config/DatabaseInitializer.java`

- [ ] **步骤 1：编写失败的 service 测试**

```java
@Test
void initializesDefaultPlanAndCompletesSteps() {
    ResearchRunPlanRepository repository = mock(ResearchRunPlanRepository.class);
    when(repository.replaceForRun(eq(501L), anyList())).thenAnswer(invocation -> invocation.getArgument(1));
    when(repository.update(any(ResearchRunPlanStep.class))).thenAnswer(invocation -> invocation.getArgument(0));

    ResearchRunPlanService service = new ResearchRunPlanService(repository);

    List<ResearchRunPlanStep> steps = service.initializeDefaultPlan(501L, 3);
    assertEquals(Arrays.asList("plan_sources", "fetch_sources", "classify_events", "extract_evidence", "compose_brief", "summarize_run"),
            steps.stream().map(ResearchRunPlanStep::getStepId).collect(Collectors.toList()));
    assertEquals("PENDING", steps.get(0).getStatus());
    assertEquals(1, steps.get(0).getMaxAttempts());

    ResearchRunPlanStep completed = service.complete(steps.get(0), "planned 3 sources", 3);

    assertEquals("COMPLETED", completed.getStatus());
    assertEquals("planned 3 sources", completed.getOutputSummary());
    assertEquals(3, completed.getProgressDelta());
}
```

- [ ] **步骤 2：运行测试并确认 RED**

运行： `cd backend && mvn -pl finscope-service -Dtest=ResearchRunPlanServiceTest test`

预期：编译失败，因为 `ResearchRunPlanStep` 和 `ResearchRunPlanService` 不存在。

- [ ] **步骤 3：实现 domain、repository、service 和 schema**

实现：

```java
public class ResearchRunPlanStep {
    private Long id;
    private Long researchRunId;
    private String stepId;
    private String title;
    private String stepType;
    private String executor;
    private String status;
    private List<String> dependencies = Collections.emptyList();
    private String inputSummary;
    private String outputSummary;
    private String errorType;
    private String errorMessage;
    private boolean fallbackUsed;
    private String fallbackReason;
    private String terminationReason;
    private int attempt;
    private int maxAttempts = 1;
    private int progressDelta;
    private LocalDateTime startedAt;
    private LocalDateTime endedAt;
    private LocalDateTime createdAt;
    private LocalDateTime updatedAt;
    private String metadataJson;
    // getters and setters
}
```

`ResearchRunPlanService.initializeDefaultPlan` 创建六个有序步骤：`plan_sources`、`fetch_sources`、`classify_events`、`extract_evidence`、`compose_brief`、`summarize_run`。

- [ ] **步骤 4：运行测试并确认 GREEN**

运行： `cd backend && mvn -pl finscope-service -Dtest=ResearchRunPlanServiceTest test`

预期：测试通过。

## 任务 2：ResearchService 集成

**文件：**
- 修改： `backend/finscope-domain/src/main/java/com/finscope/domain/research/ResearchRunPlan.java`
- 修改： `backend/finscope-service/src/main/java/com/finscope/service/research/ResearchService.java`
- 修改： `backend/finscope-service/src/test/java/com/finscope/service/research/ResearchServiceHarnessTest.java`

- [ ] **步骤 1：编写失败的 integration-style service 断言**

添加到现有 `ResearchServiceHarnessTest`：

```java
ResearchRunPlanService planService = mock(ResearchRunPlanService.class);
List<ResearchRunPlanStep> initializedSteps = defaultSteps();
when(planService.initializeDefaultPlan(501L, 3)).thenReturn(initializedSteps);
when(planService.complete(any(ResearchRunPlanStep.class), anyString(), anyInt()))
        .thenAnswer(invocation -> invocation.getArgument(0));
when(planService.fail(any(ResearchRunPlanStep.class), anyString(), anyString()))
        .thenAnswer(invocation -> invocation.getArgument(0));
ReflectionTestUtils.setField(service, "researchRunPlanService", planService);

ResearchRunPlan plan = service.createRun(runDate, Collections.singletonList(ResearchEnums.THEME_MARKET), 3, true);

assertEquals(6, plan.getPlanSteps().size());
verify(planService).initializeDefaultPlan(501L, 3);
verify(planService, atLeastOnce()).complete(any(ResearchRunPlanStep.class), anyString(), anyInt());
```

- [ ] **步骤 2：运行 RED**

运行： `cd backend && mvn -pl finscope-service -Dtest=ResearchServiceHarnessTest test`

预期：编译失败，因为缺少 `ResearchRunPlan.getPlanSteps()` 和 `researchRunPlanService` 集成。

- [ ] **步骤 3：实现集成**

变更：

```java
ResearchRunPlan plan = new ResearchRunPlan();
plan.setRun(saved);
plan.setPlannedSources(plannedSources);
plan.setPlanSteps(planSteps);
return plan;
```

在 `execute` 中，围绕现有操作更新聚合步骤。

- [ ] **步骤 4：运行 GREEN**

运行： `cd backend && mvn -pl finscope-service -Dtest=ResearchServiceHarnessTest,ResearchRunPlanServiceTest test`

预期：两个测试都通过。

## 任务 3：API 详情响应

**文件：**
- 修改： `backend/finscope-web/src/main/java/com/finscope/web/response/ResearchRunDetailResponse.java`
- 修改： `backend/finscope-web/src/main/java/com/finscope/web/controller/ResearchController.java`
- 修改： `backend/finscope-web/src/test/java/com/finscope/web/FinScopeApiIntegrationTest.java`

- [ ] **步骤 1：编写失败的 API 断言**

向 research run API 测试新增断言：

```java
mvc.perform(get("/api/research/runs/1"))
        .andExpect(status().isOk())
        .andExpect(jsonPath("$.planSteps").isArray())
        .andExpect(jsonPath("$.planSteps[*].stepId").value(hasItem("plan_sources")))
        .andExpect(jsonPath("$.planSteps[*].stepId").value(hasItem("fetch_sources")));
```

- [ ] **步骤 2：运行 RED**

运行： `cd backend && mvn -pl finscope-web -Dtest=FinScopeApiIntegrationTest#researchRunExecutesEndToEndFromThemes test`

预期：测试失败，因为缺少 `planSteps`。

- [ ] **步骤 3：返回 planSteps**

将 `ResearchRunPlanService` 注入 `ResearchController`，并把 `researchRunPlanService.findByRunId(id)` 传入 `ResearchRunDetailResponse`。

- [ ] **步骤 4：运行 GREEN**

运行： `cd backend && mvn -pl finscope-web -Dtest=FinScopeApiIntegrationTest#researchRunExecutesEndToEndFromThemes test`

预期：测试通过。

## 任务 4：Research 标签页 UI

**文件：**
- 修改： `frontend/src/shared/types/index.ts`
- 修改： `frontend/src/features/research/ResearchView.tsx`
- 修改： `frontend/src/App.test.tsx`

- [ ] **步骤 1：编写失败的 UI 测试**

向 `/api/research/runs/1` 添加 plan step mock data，打开 Research view，再打开该 run，并断言：

```ts
expect(await screen.findByText('Plan steps')).toBeInTheDocument();
expect(screen.getByText('规划来源')).toBeInTheDocument();
expect(screen.getByText('抓取来源')).toBeInTheDocument();
expect(screen.getByText('fallback: LLM_UNCONFIGURED')).toBeInTheDocument();
```

- [ ] **步骤 2：运行 RED**

运行： `cd frontend && npm test -- App.test.tsx`

预期：测试失败，因为尚未渲染 plan steps 和增强后的 trace metadata。

- [ ] **步骤 3：实现 UI**

新增：

```ts
export type ResearchRunPlanStep = {
  id?: number;
  researchRunId?: number;
  stepId: string;
  title: string;
  stepType?: string;
  executor?: string;
  status: string;
  dependencies?: string[];
  outputSummary?: string;
  errorType?: string;
  errorMessage?: string;
  fallbackUsed?: boolean;
  fallbackReason?: string;
  terminationReason?: string;
  attempt?: number;
  maxAttempts?: number;
  progressDelta?: number;
  startedAt?: string;
  endedAt?: string;
};
```

在 planned sources 上方渲染 `detail.planSteps`。

- [ ] **步骤 4：运行 GREEN**

运行： `cd frontend && npm test -- App.test.tsx`

预期：测试通过。

## 任务 5：完整验证

- [ ] **步骤 1：运行后端定向测试**

运行： `cd backend && mvn -pl finscope-service,finscope-web -Dtest=ResearchRunPlanServiceTest,ResearchServiceHarnessTest,FinScopeApiIntegrationTest#researchRunExecutesEndToEndFromThemes test`

预期：选定后端测试通过。

- [ ] **步骤 2：运行前端测试**

运行： `cd frontend && npm test -- App.test.tsx`

预期：选定前端测试通过。

- [ ] **步骤 3：运行前端构建**

运行： `cd frontend && npm run build`

预期：TypeScript 和 Vite 构建通过。

- [ ] **步骤 4：检查 diff**

运行： `git diff --stat`

预期：变更仅限 第一阶段 文件以及预先存在的用户修改。

- [ ] **步骤 5：验证前后端契约**

检查 `research_run_plan` 已由后端持久化，`ResearchRunDetailResponse.planSteps` 返回持久化步骤，`frontend/src/shared/types/index.ts` 声明相同 shape，`ResearchView` 渲染这些步骤，且 `App.test.tsx` 断言可见计划和 trace metadata。

## 自检

规格覆盖：

- 持久化计划状态：任务 1。
- ResearchService 集成：任务 2。
- 详情 API：任务 3。
- Research 标签页展示：任务 4。
- 验证：任务 5。
- 前后端契约：API response、TypeScript types、view rendering 和测试围绕 `planSteps` 保持同步。

占位符扫描：没有未解决的占位符。

类型一致性：

- `ResearchRunPlanStep` 在后端 domain、response 和前端类型中保持一致使用。
- `planSteps` 在 API 和 UI 中保持一致使用。
