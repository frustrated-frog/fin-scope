# 文章抓取 SSE 双通道实现计划

> **给智能体执行者：** 必须使用子技能：使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`，按任务逐项执行本计划。步骤使用复选框（`- [ ]`）语法跟踪。

**目标：** 为文章 URL 抓取建立 SSE 实时主通道、数据库事实状态和轮询恢复兜底。

**架构：** Service 层定义与 Web 无关的任务事件发布契约，Web 层实现支持快照、多订阅者和有界缓冲的 SSE 注册表。任务服务严格执行“先持久化状态，再发布事件”；前端以独立协调器管理 EventSource、低频事实轮询、断线加速轮询和终态确认。

**技术栈：** Java 8、Spring MVC `SseEmitter`、Spring JDBC、React 18、TypeScript、Vitest。

---

### 任务 1：任务进度事件契约

**文件：**
- 新增： `backend/finscope-service/src/main/java/com/finscope/service/task/TaskProgressEvent.java`
- 新增： `backend/finscope-service/src/main/java/com/finscope/service/task/TaskProgressPublisher.java`
- 测试： `backend/finscope-service/src/test/java/com/finscope/service/task/TaskProgressEventTest.java`

- [ ] 先写测试，断言事件能够从 `TaskView` 构造快照、阶段和终态，并保留 taskId/status/phase/articleId。
- [ ] 运行离线 Maven 聚焦测试，确认因类型不存在而失败。
- [ ] 实现不可变事件 DTO 和发布接口 `publish/complete`。
- [ ] 重跑测试确认通过。

### 任务 2：SSE 注册表与数据库快照

**文件：**
- 新增： `backend/finscope-web/src/main/java/com/finscope/web/sse/TaskSseRegistry.java`
- 新增： `backend/finscope-web/src/test/java/com/finscope/web/sse/TaskSseRegistryTest.java`
- 修改： `backend/finscope-web/src/main/java/com/finscope/web/controller/TaskController.java`

- [ ] 写失败测试覆盖订阅即时快照、多订阅者、订阅前事件补发、终态后关闭和最多 32 条缓冲。
- [ ] 运行测试确认缺少注册表和 stream 接口。
- [ ] 实现 `TaskSseRegistry`，订阅时通过 `UrlIngestTaskService.get` 读取事实快照。
- [ ] 新增 `GET /api/tasks/{taskId}/stream`。
- [ ] 重跑聚焦测试。

### 任务 3：任务状态迁移与发布顺序

**文件：**
- 修改： `backend/finscope-service/src/main/java/com/finscope/service/task/UrlIngestTaskService.java`
- 测试： `backend/finscope-service/src/test/java/com/finscope/service/task/UrlIngestTaskServiceTest.java`

- [ ] 写失败测试：每次 phase 先调用 Repository 再 publish；complete/fail 先落库，再读取状态、发布终态、关闭流；publisher 抛错不改变任务最终状态。
- [ ] 运行测试确认尚未发布事件。
- [ ] 注入 `TaskProgressPublisher`，封装 `persistAndPublishPhase`、`persistAndPublishTerminal`。
- [ ] 重跑测试确认顺序和错误隔离。

### 任务 4：前端双通道协调

**文件：**
- 新增： `frontend/src/features/articles/ingestTaskChannel.ts`
- 新增： `frontend/src/features/articles/ingestTaskChannel.test.ts`
- 修改： `frontend/src/features/articles/ArticleView.tsx`
- 修改： `frontend/src/shared/types/index.ts`

- [ ] 写失败测试覆盖 SSE 阶段即时更新、正常 5 秒轮询、断线 800 毫秒轮询、DONE 后事实确认、dispose 后旧回调失效。
- [ ] 运行 Vitest 确认协调器不存在。
- [ ] 实现独立 `IngestTaskChannel`，统一管理 EventSource、定时器、终态 Promise 和 generation token。
- [ ] ArticleView 用协调器替换 800ms `pollIngestTask`，保留超时按 URL 恢复逻辑。
- [ ] 重跑协调器和 ArticleView 测试。

### 任务 5：全链路验证

**文件：**
- 修改： `frontend/src/features/articles/ArticleView.test.tsx`
- 修改： `backend/finscope-web/src/test/java/com/finscope/web/FinScopeApiIntegrationTest.java`（仅在本地依赖允许时）

- [ ] 增加文章页 SSE 成功和断线恢复测试。
- [ ] 运行后端相关模块离线 Maven 测试。
- [ ] 运行前端全量测试和生产构建。
- [ ] 运行 `git diff --check`，并审查 SSE 生命周期和持久化顺序。
