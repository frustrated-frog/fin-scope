# 知识工作台导航文字对齐实现计划

> **给智能体执行者：** 必须使用子技能：使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`，按任务逐项执行本计划。步骤使用复选框（`- [ ]`）语法跟踪。

**目标：** 让知识工作台四个一级导航项的主标题与提示语在各自框体内整体居中，并形成稳定、适中的字号层级。

**架构：** 保留 `KnowledgeNavigation` 的组件结构、四等分网格、导航状态与响应式滚动行为，只修改共享样式契约。使用现有 Vitest 文件增加 CSS 回归断言，防止交叉轴回退到基线对齐或字号重新失衡。

**技术栈：** React 18、TypeScript 5.6、CSS、Vitest 2、Testing Library、Vite 5

---

## 文件职责

- `frontend/src/styles.css`：定义知识工作台导航项的布局、字体层级和移动端不换行约束。
- `frontend/src/features/knowledge/KnowledgeView.test.tsx`：验证导航行为，并增加导航样式契约回归测试。
- `docs/superpowers/specs/2026-07-13-知识工作台导航文字对齐设计.md`：本次实现必须满足的视觉边界与验收标准，不在实施中修改。

### 任务 1：居中并校准知识工作台导航文字

**文件：**
- 修改： `frontend/src/features/knowledge/KnowledgeView.test.tsx`
- 修改： `frontend/src/styles.css:358-385`
- 参考： `docs/superpowers/specs/2026-07-13-知识工作台导航文字对齐设计.md`

- [ ] **步骤 1：写入失败的 CSS 契约测试**

在 `KnowledgeView.test.tsx` 的 import 区加入：

```tsx
// @ts-expect-error Vitest runs in Node, while the app intentionally avoids shipping Node types.
import { readFileSync } from 'node:fs';
```

在文件末尾加入：

```tsx
test('centers and scales the four knowledge navigation labels', () => {
  const cwd = (globalThis as unknown as { process: { cwd: () => string } }).process.cwd();
  const styles = readFileSync(`${cwd}/src/styles.css`, 'utf8');

  expect(styles).toMatch(/\.knowledge-nav-item\s*{[^}]*align-items:\s*center;/s);
  expect(styles).toMatch(/\.knowledge-nav-item\s*{[^}]*justify-content:\s*center;/s);
  expect(styles).toMatch(/\.knowledge-nav-item\s*{[^}]*gap:\s*8px;/s);
  expect(styles).toMatch(/\.knowledge-nav-item\s*{[^}]*white-space:\s*nowrap;/s);
  expect(styles).toMatch(
    /\.knowledge-nav-item span\s*{[^}]*font-size:\s*15px;[^}]*font-weight:\s*650;[^}]*line-height:\s*1\.2;/s
  );
  expect(styles).toMatch(
    /\.knowledge-nav-item small\s*{[^}]*font-size:\s*11px;[^}]*font-weight:\s*500;[^}]*line-height:\s*1\.2;/s
  );
});
```

- [ ] **步骤 2：运行测试并确认它因旧排版规则失败**

运行：

```bash
cd frontend
npm test -- KnowledgeView.test.tsx
```

预期：FAIL；断言应看到当前 `align-items: baseline`、`gap: 9px`、主标题 `14px / 700`，并缺少 `white-space: nowrap` 和明确行高。

- [ ] **步骤 3：实现最小样式调整**

将 `frontend/src/styles.css` 中对应规则改为：

```css
.knowledge-nav-item {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  min-height: 54px;
  border: 0;
  border-bottom: 2px solid transparent;
  background: transparent;
  color: var(--knowledge-slate);
  white-space: nowrap;
}

.knowledge-nav-item span {
  font-size: 15px;
  font-weight: 650;
  line-height: 1.2;
}

.knowledge-nav-item small {
  font-size: 11px;
  font-weight: 500;
  line-height: 1.2;
}
```

不要修改导航高度、激活态、四等分网格和 `720px` 以下的横向滚动规则。

- [ ] **步骤 4：运行导航测试并确认通过**

运行：

```bash
cd frontend
npm test -- KnowledgeView.test.tsx
```

预期：`KnowledgeView.test.tsx` 全部 PASS，新增 CSS 契约测试通过。

- [ ] **步骤 5：运行前端构建门禁**

运行：

```bash
cd frontend
npm run build
```

预期：TypeScript 编译和 Vite production build 均成功，退出码为 0。

- [ ] **步骤 6：做桌面端和移动端视觉验收**

启动前端后打开 Knowledge 页面，分别检查桌面视口和不大于 `720px` 的移动视口：

```text
桌面端：四个导航项等宽；每组主标题与提示语整体位于框体正中。
移动端：导航可横向滚动；四组文字保持单行，不发生折行或裁切。
共同项：激活底线、背景、点击切换和 URL section 参数行为不变。
```

- [ ] **步骤 7：检查差异并提交实现**

运行：

```bash
git diff --check
git status --short
git diff -- frontend/src/styles.css frontend/src/features/knowledge/KnowledgeView.test.tsx
```

预期：只有计划内两个前端文件发生实现变更，`git diff --check` 无输出。

Commit:

```bash
git add frontend/src/styles.css frontend/src/features/knowledge/KnowledgeView.test.tsx
git commit -m "fix: 校准知识导航文字布局"
```
