# 扩展训练池与相对目标分批实施计划

用户已批准上文量化增强方案，要求在当前分支执行，不使用子智能体或工作树。使用 writing-plans 记录、executing-plans 在本任务独立执行。

目标：扩大模型学习范围，保持现有预算/权限展示池；保留绝对收盘预测，增加波动率标准化的市场相对收益学习；按可用日期引入行业共享特征；比较预先固定的概率融合及 Top 5 排序目标。

边界：当前缓存不含完整历史退市股票与成员变更史，不能称为无幸存者偏差全市场。行业记录按 retrieved_at/snapshot_at 生效，禁止向前回填。已经检查过的最近 60 日仅作为回归对照，不能作为新方法全新样本外证据。

- [ ] 批次一：`forecast/training_universe.py` + SnapshotStore 定向分页读取缓存，保留发现池并按稳定代码哈希选择其他股票，默认上限 1200；无预算/当前价格排序；按 as_of 截断。测试未来数据不影响选择、不同市场和短历史过滤。
- [ ] 批次二：`forecast/industry_features.py` 接受带可用日期的行业成员；按日构建同行动量、广度、相对行业动量及覆盖标志，不用未来标签决定特征成员。加入 joint_dataset 和指纹；测试未来行业快照不可回填、同行排除自身和覆盖缺失。
- [ ] 批次三：联合回归新增市场相对收益/历史波动率目标，并加回单独训练的市场收益预测恢复绝对收益；分类保持绝对上涨事件并增加固定等权概率融合候选；排序使用标准化相对收益 relevance、Top 5 截断。所有候选只在选择段决定，校准和验收段不调参。显示分目标证据。
- [ ] 批次四：发现与单股入口复用宽池快照；Java/React 透传训练池、展示池、行业覆盖和目标定义。时间不足或数据不足时保留原模型并解释，不能伪造未来结果。
- [ ] 批次五：固定 2025-12-31 历史实验截止，2026-09-04 作为已见回归对照；写真实指标和前瞻验证限制。相关测试/构建后重启服务，真实请求与页面验收，分批提交推送。

实现遵循现有模块边界，Java 使用注解字段注入、完整大括号，保留 application.yml 用户修改。每批测试后提交推送，提交主题使用英文类型和中文描述。
