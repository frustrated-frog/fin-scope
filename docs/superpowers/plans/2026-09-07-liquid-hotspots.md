# 今日热点液态玻璃实施计划

用户已批准在当前分支实施。目标是三块热点榜单拥有持续流动、可搅动并有惯性的半透明材质；四张脉冲卡片同时收敛为石墨、银白、冰青色系。

复用现有流体求解器和异步加载，无新增依赖。`FlowField` 扩展 panels 模式，一组热点共用一个画布，三个面板各自保存速度和染料状态。材质根据密度/速度梯度计算表面法线，对程序化环境光场进行折射采样，叠加随流动变化的银白薄层；Canvas 透明合成透出全局背景，DOM 文字不参与变形。这不是浏览器 DOM 截屏折射。

## 步骤与验证

- [x] 先补热点画布分组、降级点击与键盘回归测试，并确认失败。
- [x] 扩展渲染表面选择与 panels 模式；共用液态玻璃着色器，替换彩色染料与暖色背景。
- [x] 移除榜单实色底和粗彩条，建立半透明材质、文字保护层、轻量行反馈以及明暗/窄屏/减少动态适配。
- [x] 定向测试与生产构建通过后提交推送；真实 Chromium 验证持续运动、搅动后惯性、明暗对比度、窄屏裁切、离屏暂停与故障回退。
- [x] 独立代码评审、全量前端测试、必要修正、记录验收结果并提交推送。

修改限于前端视觉、首页及文档；遵守项目字段注入、完整 if/for 括号和模块职责规范，后端配置的用户未提交变更不纳入本次提交。

## 验收记录

- 实现提交：`c41f9f65`，已推送当前分支。
- 定向测试 4 个文件、14 项通过；全量前端测试 90 个文件、471 项通过；生产构建通过。主入口仍有既有的大包体提示。
- Chromium 实页确认 3 个画布分别服务背景、4 张指标卡片、3 块热点榜单；全部正常初始化。鼠标搅动与移开后惯性通过截图观察验证，明暗主题正文清晰。
- 390px 视口的页面宽度与滚动宽度均为 390px，榜单宽 358px，无横向溢出。
- 真实 WebGL 绘制计数：启用减少动态效果后在观察窗口保持 921 次；热点完全离屏后保持 27675 次，均未继续绘制。
- 主动模拟热点画布 context loss 后进入静态材质回退，新闻按钮仍能键盘聚焦；刷新后恢复渲染。无 WebGL 时 Enter 打开新闻已由组件测试覆盖。
- 独立代码评审未发现阻断问题；本批仅涉及前端视觉及文档，已检查完整条件/循环括号与组件职责。后端配置未纳入提交。
