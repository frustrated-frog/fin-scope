# FinScope Intake 信息摄入工作流设计方案

日期：2026-07-09

## 背景

当前 Sources 标签页只是一个很薄的信息源配置和手动抓取页面。抓取结果会进入现有抓取管线，并有机会直接进入 Article 工作区。对于少量人工精挑的信息源，这个模式还能工作；但一旦 FinScope 支持更多来源类型、更大的批量抓取和每天定时抓取，直接把内容塞进 Article 会制造信息爆炸。

新的方向是：把 Sources 改造成“信息源配置与抓取运行台”，同时新增一个 Intake 工作区，用来审核抓取出来的候选内容。候选内容只有经过人工 Promote，才会进入正式 Article。

## 产品原则

1. 所有抓取结果都先进入候选池。
2. 第一阶段绝不自动入 Article。
3. Agent Harness 用于预审、打分、中文决策摘要和批次总结。
4. 用户是最终决策者，人工状态决定候选内容是否进入 Article。
5. 候选内容被人工 Promote 后，必须走现有完整 Article ingest 和 Insight Card 生成链路。
6. 系统目标是减少决策负担，而不是制造更大的未读队列。

## 第一阶段范围

第一阶段实现“两段式 Agent 工作流”：

```text
Source 手动/定时抓取
  -> Fetch Batch 抓取批次
  -> Raw Candidate 候选内容落库
  -> 确定性预处理
     - 最近 3 天过滤
     - 每次抓取条数限制
     - URL/标题/正文指纹去重
     - 抽取质量元数据
  -> CandidateReviewAgent
     - 中文研究标题
     - 决策摘要
     - 核心事实
     - 为什么重要
     - 新信息判断
     - 风险/缺口
     - 分数和建议动作
  -> BatchSummaryAgent
     - 本批质量概览
     - 推荐优先审核顺序
     - 噪音/重复/低价值内容分布
  -> Intake 标签页人工审核
  -> 人工 Promote
  -> ArticleIngestCoordinator
  -> Article + Insight Card
```

第一阶段支持的信息源类型：

1. RSS 订阅源。
2. 单篇网页文章。
3. X/Twitter status 链接。
4. 网页列表页，即一个列表/栏目/首页抽取多篇文章链接。

第一阶段不做：

1. 自动入库。
2. 直接进入 Brief、Evidence、Learning 或 Topic。
3. 自定义时间窗口。时间范围固定为最近 3 天。
4. PDF、邮箱 Newsletter、需要登录的网站、强依赖 JavaScript 渲染的网站。
5. 多 Agent 审稿台。多 Agent 审稿台放到第二阶段。

## 领域模型

### Source

现有 `Source` 仍然是信息源配置根对象。第一阶段新增摄入配置字段：

```text
maxItemsPerRun: integer，默认 10
scheduleTimes: text，逗号分隔的 HH:mm，例如 "08:30,12:30,21:30"
scheduledEnabled: boolean，默认 false
```

现有 `fetchFrequencyMinutes` 保留用于兼容，但第一阶段固定时间调度不再使用它。

第一阶段 Source 类型：

```text
RSS
WEB
X_POST
WEB_LIST
```

含义：

1. `RSS`：RSS/Atom 订阅源，多条内容。
2. `WEB`：单篇网页文章，通常产出 1 条候选。
3. `X_POST`：X/Twitter status 或长文。
4. `WEB_LIST`：列表页、栏目页或网站首页，抽取多个文章链接。

### 抓取批次

新增 `fetch_batch` 表，记录一次手动或定时摄入运行。

字段：

```text
id
source_id
source_name
trigger_type              MANUAL | SCHEDULED
status                    RUNNING | COMPLETED | PARTIAL_SUCCESS | FAILED
started_at
ended_at
lookback_days             第一阶段固定为 3
max_items_requested
raw_item_count
candidate_count
agent_reviewed_count
duplicate_count
low_value_count
error_message
batch_summary_json
batch_summary_text
created_at
updated_at
```

现有 `fetch_run` 保留，用于兼容 Dashboard 的最近抓取展示。第一阶段新增 `fetch_batch` 给 Intake 使用；手动 intake fetch 同时记录兼容的 `fetch_run` 结果，避免破坏现有 Dashboard 行为。

### 摄入候选

新增 `intake_candidate` 表，保存每一条待审核候选内容。

字段：

```text
id
batch_id
source_id
source_name
source_type

original_title
original_url
original_summary
original_body
content_type
extraction_method
extraction_quality_score
published_at
fetched_at

chinese_title
decision_summary
key_facts_json
why_it_matters
novelty_judgment
risk_flags_json

agent_score
agent_recommendation
agent_reason
agent_model
agent_status              PENDING | SUCCESS | FAILED | FALLBACK
agent_error_message
agent_review_json

human_status              PENDING | PROMOTED | SAVED_FOR_LATER | SKIPPED | REJECTED
human_note
promoted_article_id
promoted_at

duplicate_of_candidate_id
duplicate_of_article_id
url_fingerprint
title_fingerprint
body_fingerprint

created_at
updated_at
```

所有非确定重复的候选内容默认 `human_status=PENDING`。Agent 的建议状态只做参考，不能直接改变人工状态。

## 状态语义

### Agent 建议状态

Agent 建议状态是机器预审结果，只供用户判断时参考：

```text
PROMOTABLE
NEED_REVIEW
LOW_VALUE
DUPLICATE
OFF_TOPIC
EXTRACTION_FAILED
```

### 人工处理状态

人工处理状态是最终处置结果：

```text
PENDING
PROMOTED
SAVED_FOR_LATER
SKIPPED
REJECTED
```

只有 `PENDING` 和 `SAVED_FOR_LATER` 可以被 Promote。候选内容 Promote 后记录 `promoted_article_id`，并且不能重复 Promote。

## Agent 预审契约

`CandidateReviewAgent` 必须输出结构化 JSON。前端直接按照这个结构渲染候选卡片。

```json
{
  "chineseTitle": "中文研究标题",
  "decisionSummary": "一句话说明这条信息值不值得入库，以及原因",
  "keyFacts": [
    "关键事实 1",
    "关键事实 2",
    "关键事实 3"
  ],
  "whyItMatters": "它对市场、行业、公司、创业或研究主题的意义",
  "noveltyJudgment": "NEW_EVENT | FOLLOW_UP | BACKGROUND | OPINION | DUPLICATE_LIKE",
  "riskFlags": [
    "发布时间不确定",
    "来源可能是转载",
    "数据缺少原始出处"
  ],
  "score": 0,
  "recommendation": "PROMOTABLE | NEED_REVIEW | LOW_VALUE | DUPLICATE | OFF_TOPIC | EXTRACTION_FAILED",
  "reason": "评分和推荐动作的理由"
}
```

要求：

1. 输出必须是中文，不受原文语言影响。
2. `decisionSummary` 必须服务于“是否入库”的决策，不写泛泛摘要。
3. `keyFacts` 在抽取质量允许时提供 2-4 条关键事实。
4. `score` 是 0-100 的整数。
5. LLM 不可用或返回 JSON 不合法时，系统生成确定性 fallback 预审结果，并设置 `agentStatus=FALLBACK`。

## 批次总结契约

`BatchSummaryAgent` 在单条候选预审完成后，总结整个抓取批次。

输出字段：

```text
summaryText
topCandidateIds
qualityNotes
noiseNotes
suggestedReviewOrder
```

批次总结的目标是让用户在打开单条候选之前，先知道这一批整体质量如何、最值得优先看的内容在哪里、噪音和重复大概有多少。

LLM 不可用时，使用确定性 fallback：

```text
本批共 N 条候选，高分 X 条，需复核 Y 条，低价值 Z 条，重复 D 条。
```

## 后端架构

### 新增服务

`IntakeService`

职责：

1. 启动某个 Source 的手动 intake fetch。
2. 创建和更新 `fetch_batch`。
3. 将 adapter 产出的 `RawItem` 转成 `intake_candidate`。
4. 执行确定性预处理。
5. 调用单条候选预审 Agent。
6. 调用批次总结 Agent。
7. 更新人工处理状态。
8. 将候选内容 Promote 到 Article。

`CandidateReviewAgent`

职责：

1. 基于 source 元数据和抽取内容构造预审提示词。
2. 通过现有 OpenAI-compatible LLM client 和 Agent Harness 模式调用模型。
3. 解析结构化 JSON。
4. 失败时返回确定性 fallback 预审结果。
5. 将调用轨迹记录到 `agent_run`。

`BatchSummaryAgent`

职责：

1. 汇总一个 batch 内已评分的候选内容。
2. 返回结构化批次总结。
3. 将调用轨迹记录到 `agent_run`。

`WebListSourceAdapter`

职责：

1. 抓取列表页。
2. 用确定性启发式抽取可能的文章链接。
3. 基于列表页 URL 解析相对链接。
4. 对选中的文章链接复用现有网页抽取路径。
5. 发布时间无法识别时保留风险元数据。

### 复用现有服务

1. `SourceAdapterRegistry` 仍然作为 adapter 选择入口。
2. `RssSourceAdapter`、`WebSourceAdapter`、`XPostSourceAdapter` 继续产出 `RawItem`。
3. `RawItemSelector` 可继续用于单次 source 运行内排序和去重。第一阶段在候选落库前增加最近 3 天过滤。
4. `ArticleIngestCoordinator` 仍然是创建正式 Article 的唯一入口。
5. `InsightCardGenerator` 继续负责 Promote 后的 Insight Card 生成。

## 确定性预处理

Agent 预审前先执行确定性规则：

1. `publishedAt` 已知且早于最近 3 天的内容不进入候选池。
2. `publishedAt` 未知的内容保留，但添加风险标记 `发布时间不确定`。
3. 候选数量受 `source.maxItemsPerRun` 限制。
4. 基于 URL 指纹检测是否已存在候选或正式 Article。
5. URL 不同但标题/正文高度相似时，标记重复风险。
6. 只有确定重复时，才直接设置 `agentRecommendation=DUPLICATE` 并将人工状态设为 `REJECTED`。
7. 重复证据不够强时，保留 `humanStatus=PENDING`，并在风险标记中提示。

系统倾向于保留边界候选，而不是静默丢弃。Intake 标签页的存在就是为了让用户最终判断。

## API 设计

### Sources

保留现有接口：

```text
GET    /api/sources
POST   /api/sources
PUT    /api/sources/{id}
DELETE /api/sources/{id}
```

Source 载荷新增字段：

```json
{
  "name": "Macro RSS",
  "type": "RSS",
  "url": "https://example.com/rss",
  "enabled": true,
  "scheduledEnabled": true,
  "scheduleTimes": "08:30,21:30",
  "maxItemsPerRun": 10,
  "credibility": 4,
  "tags": "宏观,市场"
}
```

新增手动 intake fetch 接口：

```text
POST /api/sources/{id}/intake-fetch
```

响应：

```json
{
  "batchId": 1,
  "status": "COMPLETED",
  "candidateCount": 8,
  "agentReviewedCount": 8
}
```

现有 `POST /api/sources/{id}/fetch` 继续保留，直到 UI 完全切换到 intake fetch。

### Intake

新增接口：

```text
GET  /api/intake/batches
GET  /api/intake/batches/{id}
GET  /api/intake/candidates?status=PENDING&batchId=1&sourceId=2
GET  /api/intake/candidates/{id}
POST /api/intake/candidates/{id}/status
POST /api/intake/candidates/{id}/promote
```

状态更新请求：

```json
{
  "humanStatus": "SAVED_FOR_LATER",
  "humanNote": "晚上再看"
}
```

Promote 响应：

```json
{
  "candidateId": 12,
  "articleId": 45,
  "status": "PROMOTED"
}
```

## 前端设计

### Sources 标签页

Sources 继续作为信息源管理工作区。

能力：

1. 创建信息源。
2. 编辑信息源。
3. 删除信息源。
4. 开启或关闭定时抓取。
5. 配置每次抓取条数 `maxItemsPerRun`。
6. 配置每天固定抓取时间 `scheduleTimes`。
7. 手动触发 intake fetch。
8. 展示每个 source 最近抓取批次状态。

第一阶段表单字段：

```text
名称
类型：RSS | Web Article | X/Twitter | Web List
URL
标签
可信度
每次抓取条数
是否开启定时抓取
每天抓取时间
是否启用
```

页面定位是配置工具，应保持紧凑、可操作，而不是承担候选审核。

### Intake 标签页

新增导航项：

```text
Intake
```

Intake 是每日候选审核工作区。

视图：

1. 左侧批次列表。
2. 当前批次总结显示在候选列表上方。
3. 状态过滤：待处理、稍后看、已入库、已跳过、已拒绝。
4. 候选列表默认按 `agentScore` 从高到低排序。
5. 候选详情使用展开面板或抽屉。

候选卡片字段：

```text
中文研究标题
分数和 Agent 建议动作
决策摘要
核心事实
为什么重要
新信息判断
风险/缺口
来源名称
发布时间
原文链接
人工状态
```

候选操作：

```text
入文章库
稍后看
跳过
低价值
```

Promote 成功后：

1. 候选状态变为 `PROMOTED`。
2. 显示 `promotedArticleId`。
3. 用户可以跳转到 Article 工作区。

## 定时调度设计

第一阶段只支持每天固定时间抓取。

规则：

1. 只有 `scheduledEnabled=true` 的 source 参与定时抓取。
2. `scheduleTimes` 是逗号分隔的本地时间，格式为 `HH:mm`。
3. Scheduler 周期性扫描到期 source。
4. 同一个 source 的同一个时间点，每天最多运行一次。
5. 手动抓取不受定时开关影响。
6. 抓取时间范围固定为最近 3 天。

实现：

1. 新增简单 Spring 定时轮询器。
2. 扫描 sources 和最近 `fetch_batch`。
3. 通过 `fetch_batch` 防止同一天同一时间点重复运行。

第一阶段使用简单 Spring 轮询器，不引入通用任务调度系统。

## Promote 入文章库流程

用户点击“入文章库”时：

1. 加载 candidate。
2. 校验 candidate 是否可 Promote。
3. 基于 candidate 内容构造 `RawItem`。
4. 加载原始 `Source`。
5. 调用 `ArticleIngestCoordinator.ingest(source, rawItem)`。
6. 写入 `promoted_article_id`。
7. 设置 `human_status=PROMOTED`。
8. 返回创建或命中的 Article ID。

Promote 必须幂等：

1. 如果 candidate 已经 Promote，直接返回已有 `promoted_article_id`。
2. 如果 Article ingest 判定为重复文章，只有拿到稳定 Article ID 时才将 candidate 标为 `PROMOTED`。

## 错误处理

抓取错误：

1. 没有产出任何候选时，batch 标记为 `FAILED`。
2. 部分候选成功、部分失败时，batch 标记为 `PARTIAL_SUCCESS`。
3. 错误信息记录在 batch 上。

Agent 错误：

1. Candidate 仍然可见。
2. 写入 fallback 中文决策卡片。
3. 设置 `agentStatus=FALLBACK` 或 `FAILED`。
4. 候选卡片展示风险提示。

Promote 错误：

1. Candidate 保持原人工状态。
2. UI 展示错误 toast。
3. 没有 Article ID 时，不能声称 Promote 成功。

## 测试策略

后端测试：

1. Source repository 能保存新增字段。
2. Intake fetch 能创建抓取批次和 candidates。
3. RSS 候选过滤遵守最近 3 天规则。
4. `maxItemsPerRun` 能限制候选数量。
5. 重复 URL 不制造重复 Pending 审核噪音。
6. LLM 关闭时，候选预审兜底结果可用。
7. LLM 关闭时，批次总结兜底结果可用。
8. Promote 通过现有管线创建 Article 和 Insight Card。
9. Promote 幂等。
10. Scheduler 同一天同一 source/time slot 不重复运行。

前端测试：

1. Sources 标签页能创建和编辑 intake 配置。
2. Sources 标签页能触发手动 intake fetch。
3. Intake 标签页能列出待处理 candidates。
4. Candidate card 展示中文决策字段。
5. 状态操作能更新候选列表。
6. Promote 调用 API 并展示已入库 Article 链接。
7. 已入库 candidates 不再出现在默认待处理视图。

手工验证：

1. 添加一个 RSS source，设置 `maxItemsPerRun=3`。
2. 手动运行 intake fetch。
3. 确认候选内容展示中文标题和决策摘要。
4. Promote 一条候选内容。
5. 确认 Article 标签页展示已入库 article 和 Insight Card。
6. 将一条候选标为低价值。
7. 确认它从 Pending 消失，并出现在 Rejected 或低价值过滤视图下。

## 第二阶段方向

第二阶段可以从两段式工作流演进为多 Agent 审稿台：

1. 翻译 Agent。
2. 事实抽取 Agent。
3. 新颖性/去重 Agent。
4. 投资相关性 Agent。
5. 创业/学习相关性 Agent。
6. 汇总多个判断的 Orchestrator Agent。

除非重新设计并明确确认，第二阶段也不应改变“人工才能入库”的原则。

## 验收标准

第一阶段完成时必须满足：

1. Sources 可以配置类型、URL、每次抓取条数、是否开启定时、每天定时点。
2. 手动 intake fetch 能创建抓取批次和 candidate records。
3. Candidates 经过 Agent 或确定性 fallback 预审，并以中文展示。
4. Intake 标签页支持过滤和人工状态修改。
5. 没有 candidate 会自动进入 Article。
6. 人工 Promote 能创建正常 Article 和 Insight Card。
7. 抓取、预审、Promote 失败都可见且可恢复。
8. 自动化测试覆盖核心后端和前端路径。

## 自检

1. 没有未完成标记。
2. 第一阶段和第二阶段边界清楚。
3. 自动入库明确不在第一阶段范围内。
4. 最近 3 天 lookback 固定，且不在 UI 暴露。
5. 人工状态是权威状态，Agent 建议只做参考。
6. Promote 复用现有 Article ingest 链路。
