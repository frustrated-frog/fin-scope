# 项目开发规范与代码评审清单

> 适用范围：后端服务、业务中台、对外适配服务，以及包含 Web、RPC、数据库、缓存、消息与任务的多模块项目。
>
> 本文是开发标准，不是业务方案。文中的“必须”是合入门槛，“应当”是默认做法；确有例外时，须在设计或代码评审中说明原因和替代措施。

## 1. 总则：先确定边界，再写实现

每一段代码都应能回答三个问题：它负责什么、它依赖什么、谁可以调用它。不能清楚回答的问题，不能靠增加注释或复制代码解决，应先调整边界。

开发时遵守以下优先级：

1. 正确性：契约、状态和数据一致性正确。
2. 可维护性：职责单一，变更影响范围可预测。
3. 可恢复性：异常、超时、重复和未知结果都有处理方式。
4. 可观测性：可通过日志、指标和流水定位一次请求。
5. 性能：基于数据与瓶颈优化，不以牺牲前三项换取“看起来更快”。

## 2. 工程结构与依赖规范

### 2.1 推荐模块基座

普通业务服务至少使用下列边界；按复杂度合并模块可以，但不得混淆职责。

| 模块 | 只负责 | 禁止承担 |
|---|---|---|
| `api` / `contract` | 对外接口、请求响应对象、DTO、稳定枚举 | 业务实现、数据库实体、框架配置 |
| `application` / `web` | 协议适配、参数校验、鉴权、请求编排、应用启动 | SQL、复杂领域规则、第三方报文拼装 |
| `domain` / `core` | 领域规则、状态转换、用例编排 | HTTP/RPC 框架细节、Mapper XML |
| `repository` / `manager` | 数据读写封装、聚合持久化、局部事务 | 面向用户的流程编排、远程调用 |
| `mapper` / `dao` | SQL、ORM 映射、数据库实体 | 业务判断、缓存和远程调用 |
| `integration` / `rpc` | 外部 RPC/HTTP/MQ 客户端、协议转换、错误归一化 | 上层业务决策、跨多个业务的编排 |
| `common` | 无业务归属的基础类型和通用技术能力 | 任何业务 Service、任意“方便复用”的杂项 |
| `worker` / `listener` / `schedule` | 消费、定时触发和异步入口 | 复制一套业务实现；应调用 `domain/core` |

复杂度较高且有独立部署需求时，可将同步入口、消息消费、定时任务拆成独立应用；共享核心模块，不复制业务逻辑。多模块的价值是控制依赖方向，不是增加目录数量。

### 2.2 依赖规则

默认依赖方向为：

```text
入口(application/web/worker) → domain/core → repository、integration → mapper
                                      ↓
                                api、common
```

- 下层**禁止**依赖上层；`mapper`、`integration`、`common` 禁止依赖 `web/application`。
- `api` 必须稳定且轻量，不能暴露内部实体、ORM 注解或框架对象。
- 外部服务只能经 `integration/rpc` 封装调用；业务代码禁止直接散落第三方 Client。
- `common` 只能沉淀跨领域且稳定的能力。一个工具若只被单一业务使用，留在该业务内。
- 先按业务语义组织包，再在业务包内分层；不要把全项目所有 `service`、`util`、`dto` 平铺在一起。

## 3. 设计思路与代码落点

### 3.1 先写清“输入—规则—状态—输出”

涉及新增接口、订单、任务、权益、资金、异步流程或外部系统时，编码前必须明确：

- 输入契约：字段含义、来源、是否必填、长度/范围、敏感级别、默认值。
- 领域规则：前置条件、互斥条件、核心计算与失败口径。
- 状态：内部状态、外部状态映射、允许的转换、未知状态的去向。
- 输出契约：成功、业务拒绝、处理中、系统失败分别如何表达。
- 副作用：数据写入、缓存变更、消息发送、远程调用及其顺序。

状态型业务必须用枚举或状态对象集中表达，禁止在多处散落字符串/数字判断。外部的“通信成功”与业务的“处理成功”必须分开建模；超时、无响应、验签失败等不能直接推断为成功或失败。

### 3.2 入口层必须薄

入口（Controller、Facade、Provider、Consumer、Job）只做以下事情：

1. 解析协议、校验基本参数、建立请求上下文；
2. 调用一个明确的用例/处理器；
3. 转换统一响应或决定消息确认结果。

入口层禁止出现 SQL、复杂循环、跨多个下游的长流程、第三方协议细节和大段 `try-catch`。一个入口方法若无法在短时间内读完，应将流程下沉到用例服务或 Processor。

### 3.3 复杂流程使用固定执行骨架

对于开户、下单、发奖、审批、资金操作等多步骤用例，应使用统一的 Processor/UseCase 模板：

```text
建立上下文 → 参数与权限校验 → 幂等判断 → 执行业务 → 更新状态
         → 产生可靠事件 → 构建响应 → 记录耗时与结果
```

- 模板负责异常转换、监控、统一日志、前后置拦截器和响应包装。
- 具体处理器只实现该场景的校验与业务步骤，不能绕过模板自建异常和监控逻辑。
- 处理器不得保存请求级可变状态为成员变量；请求状态放在显式 `Context` 中。
- 同一场景存在渠道、版本或策略差异时，使用策略/路由选择；禁止在一个方法中无限叠加 `if/else`。

### 3.4 按问题选择设计模式

设计模式只用于控制已经存在的变化，不为“代码看起来高级”而引入。按以下标准选择：

| 问题特征 | 推荐设计 | 实现要求 |
|---|---|---|
| 多个用例都有“校验—防重—执行—返回”的固定步骤 | 模板方法 / Processor | 固定生命周期放基类或执行器，业务子类只实现变化步骤 |
| 同一目标有多种可替换算法、渠道或规则 | 策略模式 | 每个策略声明 `supports/canApply` 和优先级，由 Spring 自动收集，禁止入口手写长分支 |
| 多条校验或候选处理按顺序执行，命中后停止 | 责任链 | 顺序必须显式；定义“继续、拒绝、命中”的返回语义；每个节点只做一个判断 |
| 对象具有明确状态及合法迁移，且会收到异步回调 | 状态机 | 状态、事件、迁移、前置状态、终态和失败状态集中定义；非法迁移必须拒绝 |
| 不同版本、渠道或灰度人群选择不同实现 | 路由 / Processor Switch | 选择逻辑集中在 Router，默认实现和无匹配行为明确，每次只能选择一个处理器 |
| 多个外部系统流程相同但协议细节不同 | 适配器 + 模板方法 | 基类封装通信、安全和错误处理，子类只负责请求与结果转换 |
| 多个入口重复日志、监控、异常转换 | AOP / 统一执行器 | 切点范围明确，统一脱敏、错误码、耗时和告警，不改变业务返回语义 |

简单 CRUD 不使用 Processor、状态机或责任链。出现三种以上稳定变体、多个有顺序的规则，或重复横切代码时再抽象；新增一种规则应只增加实现和注册，不修改主流程。

### 3.5 状态机规范

- 状态枚举只描述状态；状态迁移由独立处理器执行，不在多个 Service、Listener 中各自判断和更新。
- 处理器必须显式声明支持的“当前状态 + 事件”或目标状态。启动时应校验是否存在重复处理器和未覆盖迁移。
- 每次迁移先校验数据库当前状态，再以条件更新或版本号落库；分布式锁只能减少竞争，不能替代数据库条件。
- 一个状态处理器内只完成该次迁移必需的动作。复杂副作用通过领域事件/MQ 继续推进，避免一个处理器注入大量无关服务。
- 终态必须集中判断；成功、失败、关闭、未知等终态均应列出，不在调用方复制判断条件。
- 状态迁移日志必须包含业务单号、原状态、事件、目标状态和结果；重复事件应安全返回已有结果。

### 3.6 策略、责任链与自动装配

- 策略和责任链节点由 Spring 容器统一管理，可使用 `@Autowired List<接口>` 或自定义组合注解自动收集，禁止在工厂中手动 `new`。
- 执行顺序必须来自明确的 `order/priority`，并在初始化时排序；不能依赖 Bean 扫描顺序、Map 顺序或类名顺序。
- 策略选择采用“第一个匹配即返回”时，必须有默认策略或明确的无匹配返回；禁止静默产生半成品结果。
- 责任链节点返回值必须区分“通过继续”“拒绝停止”“未处理”。不要让 `null` 在不同链中代表不同含义。
- 单个节点不得修改与自身规则无关的数据；需要共享的数据放入类型明确的上下文。
- 新增节点必须配套顺序测试，证明不会改变已有节点的命中优先级。

### 3.7 上下文、拦截器与横切能力

- 跨多个处理步骤共享的请求、响应、用户、路由、幂等标记和临时结果，放入请求级 Context；不要扩张方法参数列表或把请求数据放到 Bean 成员变量。
- Context 的 Key 必须集中定义并提供类型化读写方法；禁止在业务代码散落字符串 Key 和无检查强制转换。
- Context 只在一次调用链内存活，不作为持久化对象、缓存对象或跨线程隐式共享对象。
- 鉴权、监控、日志、灰度、幂等和统一异常等横切能力使用拦截器/AOP/统一执行器；业务 Processor 不重复实现。
- 前置拦截器可中止执行，后置拦截器必须在成功和失败时都运行；后置拦截器异常不得掩盖原始业务异常。

### 3.8 外部系统隔离在适配层

每个下游依赖都必须有明确的 Adapter/RPC 类，负责：请求转换、超时设置、结果校验、错误映射、脱敏日志和降级策略。业务层只依赖本项目定义的语义接口或 DTO。

对协议复杂的下游（银行、支付、合作方）应提供抽象基类，统一完成：路由、流水号、签名/加解密、请求日志脱敏、网络调用、响应验签、状态映射。子类只实现该接口的请求载荷和业务结果解析。

禁止把下游的原始错误码、SDK 实体或 JSON 结构扩散到核心业务和对外 API。

### 3.9 数据、事务、缓存和消息

- 写操作必须先定义唯一业务键，并在数据库层建立唯一约束；幂等不能只依赖代码中的“先查再写”。
- 事务边界必须围绕一个本地一致性单元设置，通常在 `manager/repository` 或领域用例层；禁止将慢 RPC、文件传输、长循环放在数据库事务内。
- 一个操作包含多条必须同时成功的数据写入时，使用 `@Transactional(rollbackFor = Exception.class)`；事务方法保持可被 Spring 代理的公开调用，禁止同类内部调用导致事务失效。
- 事务内捕获异常后必须重新抛出或显式标记回滚；禁止记录日志后正常返回导致部分数据提交。
- 本地数据提交与消息发送必须考虑原子性。优先采用事务消息或 Outbox；若暂不能使用，必须设计补偿扫描与可重放机制。
- 消息消费者必须幂等、可重试、可定位；消费失败不得吞掉，无法自动恢复的消息必须进入告警或死信处理路径。
- 同类消费者应复用统一消息模板：批量遍历、空消息过滤、反序列化、字段校验、业务键加锁/防重、业务处理和失败上抛。具体 Listener 只实现消息解析和业务动作。
- 消息体必须定义独立事件 DTO 和事件版本，至少包含事件 ID、业务主键、事件类型和发生时间；禁止直接发送数据库实体。
- 重试消息应与普通业务事件区分 Topic 或事件类型，并记录重试次数、原事件 ID 和最后失败原因。
- 缓存 Key 必须包含业务前缀、对象标识和必要的场景维度；所有缓存必须明确 TTL、失效方式和数据源回退。
- 缓存更新要与数据库写入顺序一并设计。更新失败、延迟删除、异步刷新都应有容错策略，不能让缓存成为唯一事实来源。
- 高频读可采用 Cache Aside；涉及库存、余额、资格等强一致判断，缓存只能辅助，最终判断必须有权威数据源或原子能力。

### 3.10 配置驱动规范

- 业务配置使用类型化配置对象，禁止在业务代码中到处读取字符串并自行解析。
- 同一前缀下的一组线程池、缓存、下游或业务配置，优先使用 `@ConfigurationProperties` + Lombok 数据对象集中绑定；单个简单值再使用 `@Value`/配置中心注解。
- 动态配置必须先完成反序列化和业务校验，再原子替换内存中的旧值；新配置无效时继续使用上一份有效配置并告警。
- 配置监听按配置 ID 去重注册，更新时只刷新受影响的 Bean/字段；禁止配置变更触发全量无差别重建。
- 开关、优先级、时间窗、渠道规则等应配置化，但状态正确性、权限底线和数据约束不能由可随意修改的开关绕过。
- 配置对象必须有默认值、校验器和缺失时的明确行为。线上禁止因非核心展示配置缺失导致整个应用启动失败，核心安全配置除外。
- 动态配置更新必须记录配置标识、版本/时间和结果，禁止打印密钥及完整敏感配置。

### 3.11 并发与异步

- 每个线程池必须独立命名、配置核心/最大线程数、队列、拒绝策略和异常处理；关键下游不得与普通任务共用无边界线程池。
- 异步任务必须传递必要的 trace/请求上下文，明确超时和取消语义。
- 对远程调用设置连接、读写和总超时；按风险隔离线程池，并定义降级或失败返回。
- 定时任务必须可重复执行、可跳过重叠执行，并记录本次处理数量、失败数量和游标/时间窗。

## 4. 代码规范

### 4.0 首先遵循项目既有规范

新项目建立本规范后，后续代码必须保持同一种风格；在已有项目中开发时，以仓库既有的稳定写法为准。AI 生成代码不得因通用偏好擅自替换项目既有的注入方式、Lombok 使用方式、响应包装或异常处理方式。

本规范针对当前 Spring + MyBatis + Lombok 技术栈的默认约定是：依赖使用 Spring 注解注入，数据对象使用 Lombok，外围边界统一兜底。除非项目明确采用另一套规范，否则不得引入构造方法注入、手写 Getter/Setter 等与现有代码风格不一致的写法。

### 4.1 命名与对象边界

- 命名以业务语义清晰为唯一硬标准，不强制使用固定后缀。项目已有 `Facade`、`Service`、`Processor`、`Rpc`、`Mapper` 等约定时应保持一致；新建类型可采用这些名称，也可采用能准确表达职责的名称。禁止为满足后缀而产生无意义的 `XxxServiceImpl`、`XxxUtil`、`XxxManager`。
- 是否定义接口由替换需求、稳定边界和测试隔离决定，不为“接口 + Impl”形式而机械创建两份类型。
- 请求、响应、DTO、实体必须分离。`Request` 只代表调用输入，`Response` 只代表调用输出，持久化实体不得直接向外暴露。
- 方法名使用动词加业务对象：查询用 `get/query/list`，写入用 `create/update/remove`，动作使用 `open/grant/confirm` 等真实业务动词。禁止 `doHandle`、`processData`、`util` 等无语义命名。
- 枚举必须集中维护值、展示语义和必要的转换方法；禁止硬编码状态值、渠道码、错误码和魔法数字。
- 布尔变量和方法以 `is/has/can/should` 开头；集合使用复数名；单位写入变量名，例如 `timeoutMs`、`amountFen`。

### 4.2 注入与数据对象

- Spring Bean 的依赖默认使用字段注入：优先 `@Autowired`，需要按名称或 JSR-250 语义注入时使用 `@Resource`。字段保持 `private`，由 Spring 完成装配。
- **禁止**为了通用范式改用构造方法注入、手动 `new` 业务 Bean 或混用多套注入方式；只有框架限制、不可选依赖或项目已有统一约定时才可例外，并在评审中说明。
- DTO、Request、Response、事件对象和 MyBatis DAO/Domain 实体默认使用 Lombok `@Data`，需要构建器时增加 `@Builder`，需要无参/全参构造时使用相应 Lombok 注解。
- **禁止**为普通数据承载对象手写大量 `getter/setter`、`toString`、`equals/hashCode`。仅当对象存在不可变性、脱敏输出或相等性等特殊语义时，才按需要显式实现并说明原因。
- DTO/实体之间存在大量同名字段转换时，使用项目统一的 MapStruct/转换器；涉及字段语义变化、金额单位或外部协议时必须显式映射，禁止无审查地使用反射式 Bean Copy。
- 方法返回集合时优先返回空集合，不返回 `null`；数据对象的可选字段由契约说明，不能依赖调用方猜测。

### 4.3 方法与类

- 一个方法只完成一个可描述的步骤。出现“校验 + 查询 + 远程调用 + 更新 + 发消息 + 拼响应”时，必须拆分或交给 Processor 编排。
- 公开方法应先处理非法输入和快速返回分支，主流程保持线性；嵌套通常不超过三层。
- 类的依赖超过 7 个、方法超过约 80 行、同一类包含多个独立业务流程时，必须评估拆分。数字是提醒阈值，不替代职责判断。
- 不允许用反射、全局静态变量或 `ThreadLocal` 隐藏业务依赖；必要的上下文应显式传递并在请求结束清理。
- 禁止 `System.out`、空实现、空 `catch`、捕获异常后只返回 `null`，以及为了通过编译而吞掉中断异常。

### 4.4 参数、异常与返回

- 边界层必须校验必填、格式、长度、范围、权限和幂等键；领域层必须再次校验会影响正确性的业务前置条件。
- 最外围边界（Controller、Facade、Provider、消息 Consumer、Job）必须有且只有一套统一异常兜底。优先使用 AOP、全局异常处理器或 Processor 执行器集中捕获；项目没有统一组件时，才在每个入口方法使用 `try/catch`。兜底逻辑必须记录脱敏请求、业务主键和完整堆栈，并转换为统一失败响应/错误码；消息和任务入口则按框架语义抛出异常以触发重试/失败记录。
- AOP/统一执行器应区分参数错误、业务异常、并发/锁异常、下游异常和未知异常，分别映射稳定错误码；业务异常不得被全部降格为“系统异常”。
- 领域层和下游适配层不得重复捕获宽泛异常并各自拼响应。能恢复或降级的局部异常可在本层捕获；其余异常补充上下文后抛给最外围接口统一处理。
- 业务拒绝使用统一的业务异常/错误码；系统异常保留完整堆栈并对调用方返回稳定、非敏感的信息。
- 不允许以 `null` 同时表示“没有数据”“参数错误”和“调用失败”。查询无结果、处理中、业务失败、系统失败应有明确语义。
- 捕获异常必须至少完成一项：补偿、转换、附加上下文后重抛、记录并转入可恢复队列。仅记录后继续执行必须证明安全。
- 错误码是对外契约，必须集中定义、稳定、可检索；下游错误要映射为本系统语义，不应透传内部实现细节。

### 4.5 日志、监控与安全

- 类日志默认使用 Lombok `@Slf4j`，不在每个类手写 Logger 初始化；底层框架或特殊日志类别除外。
- 每个入口记录请求标识、业务主键、调用方、结果码和耗时；关键状态转换、缓存失效、消息投递、下游失败必须可检索。
- 日志字段用结构化键值表达，优先记录 `traceId`、`bizNo/orderNo`、`userId`（脱敏）、`scene`、`downstream`、`costMs`、`result`。
- 日志**禁止**输出密码、密钥、令牌、证件号、完整手机号、银行卡号、签名原文及完整敏感报文；统一使用脱敏工具。异常日志必须带堆栈。
- 为入口、下游调用、消息消费、定时任务建立成功量、失败量、延迟和超时指标；不要只依赖文本日志排障。
- 配置、密钥、开关、限流阈值必须外置并按环境管理；禁止硬编码到源码和提交到仓库。功能开关要有默认值、命名空间、负责人和清理期限。

### 4.6 数据访问

- SQL 使用参数绑定，禁止字符串拼接用户输入；动态排序、表名等不可绑定部分必须使用白名单。
- 所有写 SQL 都要明确影响行数的预期；并发更新使用版本号、条件更新或数据库锁，不能默认“最后写入者胜出”。
- 按唯一业务键查询时：0 条返回“未找到”的明确语义，1 条返回数据，大于 1 条必须记录并抛出数据异常；不能随意取第一条掩盖脏数据。
- 预期更新单条记录时必须校验影响行数等于 1；影响 0 条和多条都按异常处理。批量更新必须明确预期范围。
- 数据库枚举与 Java 枚举的转换集中在 MyBatis TypeHandler/Converter 中，禁止在每个 Service 中手写编码转换。
- 分页必须稳定排序，禁止无上限全表查询；批量写入要分批并监控单批耗时。
- 后台扫描和补偿任务优先采用“递增主键游标 + 固定批次 + 稳定排序”，避免大偏移量分页和重复/遗漏。
- 每次表结构或索引变更都要核对查询条件、唯一约束、回滚方案和历史数据兼容性。

## 5. 通用工程基座（必须具备）

以下能力应以独立、稳定的组件提供，业务代码只能调用，不得各自复制实现。

| 基座能力 | 最低标准 |
|---|---|
| 统一契约 | `Request/Response` 基类、分页模型、错误码、参数校验、版本兼容策略 |
| 请求上下文 | traceId、调用来源、业务主键、灰度/租户等必要上下文；跨线程和消息可传递 |
| 异常体系 | 业务异常、系统异常、下游异常、并发异常的统一转换和对外响应 |
| 统一执行器 | 校验、防重、业务执行、响应封装、前后置拦截、异常兜底和耗时监控；业务入口可直接复用 |
| 扩展点注册 | 策略/责任链节点自动收集、显式排序、重复检测、默认实现和启动日志 |
| 状态机 | 状态/事件/迁移模型、条件更新、非法迁移拒绝、重复事件幂等和迁移日志 |
| 日志与指标 | 结构化日志、脱敏、入口与下游耗时、失败率、告警维度 |
| 配置中心 | 类型化配置、分环境配置、动态开关、校验后原子更新、保留上一有效值、敏感配置隔离和变更审计 |
| RPC/HTTP 客户端 | 超时、重试边界、线程池隔离、熔断/降级、请求响应脱敏、错误映射 |
| 数据访问 | 连接池、事务封装、审计字段、乐观锁/条件更新、枚举 TypeHandler、分页与批处理规范 |
| 缓存 | Key 生成器、序列化、TTL、穿透/击穿保护、失效和降级约定 |
| 消息 | 统一消费模板、事件模型与版本、生产记录、幂等消费、重试/死信、延迟与积压监控 |
| 任务执行 | 命名线程池、拒绝策略、上下文传递、任务锁、执行结果与告警 |
| 测试支撑 | Mock 下游、测试数据构造器、时钟/ID/配置可替换、集成测试隔离 |

基座代码的准入标准高于业务代码：API 稳定、依赖最少、文档明确、测试充分。`common` 不是“任何代码都能放”的仓库；发现业务语义后必须迁回所属领域。

## 6. AI 开发执行规范

本节用于约束 AI 在已有项目或新项目基座上的编码行为。

### 6.1 写代码前

- 必须先阅读目标模块的 `pom.xml`/构建配置、同目录 2～3 个同类实现、统一响应、异常、日志、缓存、RPC 和测试基类。
- 必须列出可直接复用的基座能力；已有统一组件时禁止另写一套局部工具。
- 必须确认项目采用的 Spring 注入、Lombok、序列化、ORM、测试和包组织方式，并保持一致。
- 修改现有项目时，项目已明确的局部规范优先于通用偏好；遇到冲突先指出，不擅自“现代化改造”。

### 6.2 生成代码时

- 默认使用 `@Autowired`/`@Resource` 字段注入，不擅自生成构造方法注入。
- 数据对象默认使用 Lombok，不批量生成 Getter/Setter、构造器和 `toString`。
- 使用现有统一响应、异常码、AOP/Processor、RPC 包装、缓存 Key 生成器、消息模板和线程池，不绕过基座直接调用底层客户端。
- 不为单一实现机械创建接口、`Impl`、Factory、Strategy；只有出现稳定变化点时才抽象。
- 不新增依赖、框架、模块或通用组件，除非现有能力确实无法满足且已说明引入理由。
- 不顺手重命名、格式化或重构无关代码；改动保持在需求边界内。

### 6.3 生成后自检

- 检查是否混入构造方法注入、手写 Getter/Setter、固定后缀命名或重复工具类。
- 检查最外围异常是否由既有统一机制接管，内部异常是否错误地吞掉或转换为 `null`。
- 检查状态、错误码、缓存 Key、Topic、配置项和下游地址是否存在硬编码。
- 检查数据库写入影响行数、幂等、缓存失效、消息重试和下游超时是否遵循基座约定。
- 将新增代码与同模块代表性实现进行一次风格对照，再执行项目测试与静态检查。

## 7. 测试标准

- 新增或修改领域规则、状态转换、金额/资格计算、缓存失效、幂等和错误映射时，必须有自动化单元测试。
- 每个关键用例至少覆盖：正常路径、非法输入、业务拒绝、下游失败/超时、重复请求和边界值。
- 适配层测试必须验证请求映射、响应映射和异常转换；不得只验证 Mock 被调用过。
- 消息消费者、定时任务和并发逻辑必须覆盖重复执行、部分失败、重试和幂等场景。
- 状态机测试必须覆盖合法迁移、非法迁移、重复事件和终态；策略/责任链测试必须覆盖优先级和无匹配行为。
- 外围统一异常测试必须验证不同异常类型映射到正确错误码；动态配置测试必须验证非法配置不会覆盖上一有效值。
- 关键模块的行覆盖率应不低于 80%，但合入判断以关键分支是否覆盖为准，不能以无意义测试堆覆盖率。
- CI 必须执行测试。构建脚本中的跳过测试选项只能用于明确的本地或临时场景，不能成为主构建默认。

## 8. 代码评审清单

### 8.1 架构与设计

- [ ] 改动所在层和模块合理，没有逆向依赖或跨层直连。
- [ ] 对外契约、内部 DTO、持久化实体与下游模型已隔离。
- [ ] 复杂流程有明确编排入口，横切逻辑未复制到各业务方法。
- [ ] 设计模式与问题匹配：简单逻辑没有过度设计，扩展型逻辑没有退化为长 `if/else`。
- [ ] 策略/责任链顺序、停止条件、默认实现和无匹配结果均明确；新增实现不需要修改主流程。
- [ ] 状态型业务集中定义合法迁移、终态和重复事件行为，没有在多个入口直接改状态。
- [ ] 请求级共享数据使用类型化 Context，没有使用 Bean 成员变量或散落字符串 Key。
- [ ] 状态、错误码、渠道/策略差异已集中建模，没有散落硬编码。
- [ ] 外部系统访问经 Adapter/RPC 封装，超时、失败、降级语义明确。

### 8.2 正确性与一致性

- [ ] 参数、权限、业务前置条件和幂等键均已校验。
- [ ] 数据库唯一约束、条件更新或锁与代码并发策略一致。
- [ ] 事务不包含慢远程调用；本地写入与消息/缓存副作用有一致性方案。
- [ ] 缓存 Key、TTL、失效与回退逻辑完整，不会串业务场景。
- [ ] MQ/任务复用统一处理模板，消息有事件 ID/版本，处理可重试且幂等，失败不会静默丢失。
- [ ] 动态配置有类型、默认值和校验器；无效更新不会覆盖上一份有效配置。

### 8.3 代码质量与安全

- [ ] 命名能表达业务意图；方法和类职责单一；没有无意义抽象。
- [ ] Spring Bean 使用项目约定的 `@Autowired`/`@Resource` 注入，没有混入构造方法注入或手动创建 Bean。
- [ ] DTO、Request、Response、事件与 DAO 实体使用 Lombok `@Data`，没有重复手写 Getter/Setter。
- [ ] 复用了项目既有响应、异常、RPC、缓存、消息和日志基座，没有重复造轮子或擅自引入依赖。
- [ ] 异常没有被吞掉，返回值不以 `null` 承载多种语义。
- [ ] 最外围边界由 AOP/全局处理器/统一执行器兜底；参数、业务、并发、下游和未知异常映射正确，没有重复捕获或漏记。
- [ ] 日志包含定位主键和耗时，敏感信息已脱敏，异常有堆栈。
- [ ] 无硬编码密钥、环境地址、开关或未经审计的默认值。
- [ ] SQL 参数化、分页受控、批量操作受控，索引与查询条件匹配。

### 8.4 测试

- [ ] 新增规则与关键分支有测试，测试名能说明行为与预期。
- [ ] 覆盖正常、失败、边界、重复和下游异常路径。
- [ ] Mock 没有掩盖真实契约错误；需要时有集成测试验证序列化、SQL 或消息配置。
- [ ] 本地/CI 测试实际执行且通过。

## 9. 从三个项目提炼出的取舍

本规范综合审阅了 `jdt-dd-expense`、`fin-bt-activity` 和 `bank-store` 的多模块结构、入口实现、状态处理、RPC 封装、消息/缓存使用、测试目录与配置。主要证据与形成的通用规范如下：

| 项目中的实现 | 提炼后的通用规范 |
|---|---|
| `jdt-dd-expense` 将 Web、Listener、Worker 拆成独立运行入口，共享 Service | 同步入口、消息入口、任务入口可独立部署，但业务核心必须复用，不能复制 |
| `jdt-dd-expense` 的 `ExpenseStateMachineHandler`、状态处理器和操作上下文 | 状态型业务使用显式状态机；迁移、终态、幂等和上下文集中管理 |
| `jdt-dd-expense` 的缓存更新事件、重试 Listener 和可动态关闭的消费开关 | 缓存一致性、失败重试、消费降级是消息基座能力，不散落为临时补丁 |
| `fin-bt-activity` 的 `JsfProviderAspect` | 外围边界统一完成日志、监控、异常分类和响应转换，业务入口保持简洁 |
| `fin-bt-activity` 的准入责任链、弹窗责任链和推荐策略集合 | 扩展规则交由 Spring 自动收集并按显式优先级执行，新增规则不修改主流程 |
| `fin-bt-activity` 的消息抽象 Listener 与 `ProcessMsgUtil` | 同类消费者共享“解析—校验—防重/加锁—处理”的固定模板 |
| `fin-bt-activity` 的配置注入器、校验器和变更监听 | 动态配置必须类型化、校验后更新、监听去重，并保留最后有效值 |
| `bank-store` 的 API、App、核心账户 Service、银行 Front 分工 | 用户场景编排、核心业务事实、外部协议适配属于不同边界，不相互渗透 |
| `bank-store` 的 `ProcessorFactory`、`AbstractModuleProcessor`、Context 和 Interceptor | 复杂用例使用统一执行生命周期，横切能力通过执行器和拦截器收口 |
| `bank-store` 的 `AbstractRpc`、`RpcAgent` 和命令封装 | 下游调用统一支持同步/异步、隔离、监控、异常转换和降级，不直接裸调 Client |
| `bank-store-front` 的银行通信抽象基类 | 相同通信流程使用模板方法，渠道子类只实现协议差异 |
| `bank-store-service` 的 Manager、Mapper、TypeHandler 和游标式扫描 | 数据访问封装唯一性与影响行数规则；枚举集中映射；后台任务稳定增量扫描 |

项目中的做法不直接等于规范。以下写法即使在存量代码中出现，也不应作为新项目基线：捕获远程异常后返回 `null`、不脱敏打印完整出入参、关闭 RPC 超时/熔断、默认跳过测试、复制多份相同基础框架、用分布式锁替代数据库幂等，以及让一个状态处理器承担过多无关职责。

最终应当继承的是稳定的设计思想：明确边界、固定主干、开放扩展点、集中处理横切能力、显式管理状态与失败；具体类名、后缀和业务目录不照搬。
